# This file is automatically generated

name: Auto Builder CI

on:
  workflow_dispatch:
  repository_dispatch:
  schedule:
    - cron: "0 0 * * 0"

jobs:
  alpine-edge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: main
          fetch-depth: 0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
        id: buildx

      - name: Docker Login
        uses: docker/login-action@v1
        with:
          username: vcxpz
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Github Login
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "github-actions"

      - name: Build And Push The Docker Image
        run: |
          docker buildx build \
            --platform=linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64/v8,linux/386,linux/ppc64le,linux/s390x \
            --output "type=image,push=true" \
            --build-arg TAG=edge \
            --build-arg BUILD_DATE="$(date +%Y-%m-%d)" \
            --tag vcxpz/apk-packager:edge \
            --file Dockerfile .

  alpine-3-13:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: main
          fetch-depth: 0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
        id: buildx

      - name: Docker Login
        uses: docker/login-action@v1
        with:
          username: vcxpz
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Github Login
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "github-actions"

      - name: Build The Docker Image Locally
        run: |
          docker buildx build \
            --platform=amd64 \
            --output "type=docker" \
            --build-arg BUILD_DATE="$(date +%Y-%m-%d)" \
            --tag vcxpz/ci-build:ci-build \
            --build-arg TAG=3.13 \
            --file Dockerfile .

      - name: Build And Push The Docker Image
        run: |
          docker buildx build \
            --platform=linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64/v8,linux/386,linux/ppc64le,linux/s390x \
            --output "type=image,push=true" \
            --build-arg TAG=3.13 \
            --build-arg BUILD_DATE="$(date +%Y-%m-%d)" \
            --tag vcxpz/apk-packager:latest \
            --tag vcxpz/apk-packager:3.13 \
            --file Dockerfile .

      - name: Get New Package Versions From Image
        run: |
          docker run --rm --entrypoint /bin/sh -v ${PWD}:/tmp vcxpz/ci-build:ci-build -c '\
            apk info -v | sort >/tmp/package_versions.txt'

      - name: Commit And Push Changes To Github
        run: |
          git add -A
          git commit -m "Bot Updating Files" || true
          git push || true

      - name: Sync README.md With Docker Hub
        uses: peter-evans/dockerhub-description@v2
        with:
          username: vcxpz
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: vcxpz/apk-packager

  alpine-3-12:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: main
          fetch-depth: 0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
        id: buildx

      - name: Docker Login
        uses: docker/login-action@v1
        with:
          username: vcxpz
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Github Login
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "github-actions"

      - name: Build And Push The Docker Image
        run: |
          docker buildx build \
            --platform=linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64/v8,linux/386,linux/ppc64le,linux/s390x \
            --output "type=image,push=true" \
            --build-arg TAG=3.12 \
            --build-arg BUILD_DATE="$(date +%Y-%m-%d)" \
            --tag vcxpz/apk-packager:3.12 \
            --file Dockerfile .

